# F5 BIG-IP SSL/TLS Offloading Guide

## Introduction

SSL/TLS offloading transfers the cryptographic processing of encrypted traffic from backend servers to BIG-IP. This improves server performance, centralizes certificate management, and enables traffic inspection and manipulation.

## SSL Offloading Architectures

### Client SSL Termination (SSL Offloading)
BIG-IP terminates SSL from clients and sends unencrypted traffic to servers.

Traffic flow:
1. Client connects via HTTPS (encrypted)
2. BIG-IP decrypts traffic
3. BIG-IP sends HTTP (unencrypted) to servers
4. Server responds via HTTP
5. BIG-IP encrypts response
6. Encrypted response sent to client

Benefits:
- Reduced server CPU load
- Centralized certificate management
- Traffic inspection capability
- iRule processing possible

### SSL Bridging (Re-encryption)
BIG-IP terminates client SSL and creates new SSL connections to servers.

Traffic flow:
1. Client connects via HTTPS
2. BIG-IP decrypts (Client SSL)
3. BIG-IP re-encrypts (Server SSL)
4. Server receives encrypted traffic
5. Response follows reverse path

Use cases:
- End-to-end encryption required
- Different certificates for internal/external
- Different cipher requirements

### SSL Passthrough
BIG-IP forwards encrypted traffic without decryption.

Limitations:
- No traffic inspection
- No content manipulation
- Limited to L4 load balancing

## SSL Profiles

### Client SSL Profile
Handles incoming SSL connections from clients.

Key settings:
- Certificate and key (required)
- Certificate chain (intermediate certs)
- Cipher suite configuration
- SSL/TLS version settings
- Client certificate authentication
- Session caching

Configuration:
```bash
# Create Client SSL profile
tmsh create ltm profile client-ssl my_clientssl {
    cert my_cert.crt
    key my_cert.key
    chain my_chain.crt
    ciphers 'TLSv1.2:!SSLv3:!ADH:!EXPORT'
}
```

### Server SSL Profile
Handles outgoing SSL connections to backend servers.

Key settings:
- Server certificate verification
- Trusted CA certificates
- Server name indication (SNI)
- SSL/TLS version settings
- Cipher configuration

Configuration:
```bash
# Create Server SSL profile
tmsh create ltm profile server-ssl my_serverssl {
    ca-file trusted_ca.crt
    server-name example.com
    ciphers 'TLSv1.2:!SSLv3'
}
```

## Certificate Management

### Certificate Types
- Self-signed - For testing only
- CA-signed - Production standard
- Wildcard - Covers multiple subdomains
- SAN (Subject Alternative Name) - Multiple domains on one cert
- EV (Extended Validation) - Highest trust level

### Certificate and Key Import
```bash
# Import certificate
tmsh install sys crypto cert my_cert from-local-file /var/tmp/cert.crt

# Import key
tmsh install sys crypto key my_key from-local-file /var/tmp/key.key

# Verify certificate details
tmsh list sys crypto cert my_cert
```

### Certificate Chain Configuration
Proper chain order (bottom to top):
1. Server certificate
2. Intermediate CA(s)
3. Root CA (optional - usually in client trust store)

```bash
# Create certificate bundle
cat server.crt intermediate.crt > bundle.crt

# Import as chain
tmsh install sys crypto cert my_chain from-local-file /var/tmp/bundle.crt
```

## Cipher Suite Configuration

### Cipher String Syntax
```
# Format: keyword[:keyword]
TLSv1.2          # TLS 1.2 ciphers only
!SSLv3           # Exclude SSL 3.0
!ADH             # Exclude anonymous DH
!EXPORT          # Exclude export ciphers
!NULL            # Exclude null ciphers
@STRENGTH        # Order by strength
```

### Recommended Cipher Strings

Modern (TLS 1.2+):
```
TLSv1.2+TLSv1.3:!aNULL:!eNULL:!EXPORT:!DES:!RC4:!3DES:!MD5:!PSK
```

Compatibility (wider support):
```
ECDHE+AESGCM:DHE+AESGCM:ECDHE+AES:DHE+AES:!aNULL:!MD5:!DSS
```

### View Available Ciphers
```bash
tmsh list ltm profile client-ssl my_clientssl ciphers
tmsh run util bash -c "openssl ciphers -v 'CIPHER_STRING'"
```

## TLS Version Configuration

### Recommended Settings
```bash
# Client SSL profile - disable older versions
tmsh modify ltm profile client-ssl my_clientssl {
    options { dont-insert-empty-fragments no-tlsv1 no-tlsv1.1 }
}
```

Version settings:
- no-sslv2 - Disable SSL 2.0 (always)
- no-sslv3 - Disable SSL 3.0 (recommended)
- no-tlsv1 - Disable TLS 1.0 (recommended)
- no-tlsv1.1 - Disable TLS 1.1 (recommended)

## Client Certificate Authentication

Mutual TLS (mTLS) requires clients to present certificates.

Configuration:
```bash
# Require client certificate
tmsh modify ltm profile client-ssl my_clientssl {
    client-cert-ca ca_bundle.crt
    peer-cert-mode require
}
```

Modes:
- ignore - Don't request client cert
- request - Request but don't require
- require - Require valid client cert

Access client cert in iRule:
```tcl
when CLIENTSSL_CLIENTCERT {
    set subject [SSL::cert subject]
    set issuer [SSL::cert issuer]
}
```

## SSL Session Management

### Session Caching
Improves performance by reusing session parameters.

```bash
tmsh modify ltm profile client-ssl my_clientssl {
    cache-size 262144
    cache-timeout 3600
}
```

### Session Tickets
Alternative to server-side session caching.

```bash
tmsh modify ltm profile client-ssl my_clientssl {
    session-ticket enabled
}
```

## OCSP Stapling

Improves certificate validation performance.

```bash
# Create OCSP stapling profile
tmsh create ltm profile ocsp-stapling my_ocsp {
    dns-resolver my_resolver
    sign-hash sha256
}

# Attach to client SSL profile
tmsh modify ltm profile client-ssl my_clientssl {
    ocsp-stapling enabled
    status-age 86400
}
```

## SSL Visibility (Forward Proxy)

Intercept and inspect outbound SSL traffic.

Components:
- Forward Proxy SSL profile
- CA certificate for signing
- Bypass rules (for sensitive sites)

Use cases:
- Outbound traffic inspection
- DLP integration
- Threat protection

## Perfect Forward Secrecy (PFS)

Ensures session keys can't be compromised if server key is leaked.

Enable by using ECDHE or DHE cipher suites:
```bash
tmsh modify ltm profile client-ssl my_clientssl {
    ciphers 'ECDHE+AESGCM:ECDHE+AES:DHE+AESGCM:DHE+AES'
}
```

## Performance Optimization

### Hardware Acceleration
BIG-IP platforms include SSL acceleration hardware.

Monitor performance:
```bash
tmsh show sys performance ssl
```

### Optimization Tips
1. Enable session caching or tickets
2. Use ECDSA certificates (faster than RSA)
3. Prefer ECDHE over DHE for key exchange
4. Use appropriate key sizes (RSA 2048 sufficient)
5. Enable OCSP stapling
6. Consider TLS 1.3 for improved handshake

## Troubleshooting

### Common Issues
1. Certificate chain incomplete - Add intermediate certs
2. Certificate mismatch - Verify hostname matches
3. Cipher mismatch - Check client/server cipher overlap
4. Protocol mismatch - Enable compatible TLS versions

### Debug Commands
```bash
# Check SSL handshake details
tmsh run util tcpdump -ni external -Xvv port 443

# View SSL statistics
tmsh show ltm profile client-ssl my_clientssl

# Check certificate expiration
openssl s_client -connect host:443 2>/dev/null | openssl x509 -noout -dates
```

## Best Practices

1. Use TLS 1.2 or higher only
2. Implement strong cipher suites
3. Enable Perfect Forward Secrecy
4. Maintain proper certificate chains
5. Monitor certificate expiration
6. Enable OCSP stapling
7. Use appropriate key sizes (2048+ bit RSA, 256+ bit ECDSA)
8. Regularly update to address vulnerabilities
9. Document certificate renewal procedures
10. Test SSL configuration with external scanners
